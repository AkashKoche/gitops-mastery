apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-first-app-ui
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/AkashKoche/gitops-mastery
    targetRevision: main
    path: day-2
    destination:
      server: https://kubernetes.default.svc
      namespace: default
    syncPolicy:
    - Prune=true
    - CreateNamespace=true
